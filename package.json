{
  "name": "gitdir",
  "version": "1.0.0",
  "description": "Generates a directory website listing local git bare repositories",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "canvasplay",
  "license": "MIT",
  "dependencies": {
    "ejs": "^2.5.7",
    "fs-extra": "^5.0.0"
  }
}
